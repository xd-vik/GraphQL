import { mutations } from "./mutations";
import { queries } from "./queries";
import { resolvers } from "./resolvers";
import { typedef } from "./typedef";

export const User = { mutations, queries, resolvers, typedef };
